import React, { Component } from 'react'

class Users extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default Users

// loop over users array (hard code for now)
// create UserCard for each
// UserCard should take a users prop
